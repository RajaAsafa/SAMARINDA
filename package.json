{
  "name": "samarinda-terbaru-root",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "axios": "^1.16.0"
  }
}
