{
  "name": "samarinda-terbaru-root",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r dist ../dist"
  },
  "dependencies": {
    "axios": "^1.16.0"
  }
}
